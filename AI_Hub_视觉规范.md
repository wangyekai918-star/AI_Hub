# AI Hub 视觉规范：专家、技能、数字员工与全局配色

> 更新日期：2026-09-21。适用项目：AI Hub Demo，本地与在线预览共用。
> 本次更新：专家顶部独立圆角板块、充电场景纹理、固定问候气泡、彩色利益点与独立视频封面。
> 面向前端开发与 AI 编程工具。依据本轮已确认的设计要求，以及当前 `index.html`、`style.css`、`app.js` 和本地 SVG 整理。
> 本文是交付规范，不要求重新设计页面，也不要求更换现有技术栈。

## 1. AI 编程工具先读这些规则

1. **主色为 `#5552FF`，页面底色为 `#EDF1FC`。** 不套用 AI 管家其他项目的蓝色主题，也不直接采用 HTML 原型的视觉样式。
2. **区分三个层级：页面底色、卡片底色、图标底座颜色。** 彩色渐变主要用于角色 / 技能的圆形图标底座；目录卡片本身使用白色。
3. 专家、专家团、技能、数字员工共享四组业务配色，**不按“专家一种色、技能另一种色、员工第三种色”分配**。
4. 角色 / 技能的身份图标采用 **Solar Bold 面型图形＋不透明彩色渐变底座＋白色图形**；不同功能根据名称选图，不随机复用一个通用图标。
5. **左侧列表图标底座 36×36px、纯圆，内部图形 19×19px。** 不加描边、顶部 1px 高光、内阴影或底部投影。
6. 不把上述 36px 套到所有场景；能力目录、对话头像和顶部装饰图标有各自尺寸，见第 4 节。
7. **所有新增图标先下载为本地独立 SVG，再引用。** 不使用运行时 Iconify / CDN / Yesicon 网络地址，不恢复集中式图标集合。
8. 名称、颜色和图标通过固定映射维护。筛选、排序、搜索后，同一对象的图标和底色保持不变。
9. 导航选中态统一为浅紫底、紫色文字；**不把整行背景改成该角色的蓝 / 橙 / 青绿渐变**。
10. “专家能帮你做点什么？”中的案例只是填入快捷入口，**不保留选中态，不使用 `aria-pressed`**。案例固定为 **12px 圆角矩形**，右侧独立视频封面为 **22px圆角卡片**。
11. 用户最新明确要求优先于本文；本文优先于旧截图、过时注释和原型视觉。业务流程参考最新原型，不能据原型把已确认的高保真样式改回去。
12. 默认只修改本地文件。仅在用户明确要求上传时提交、推送或部署 GitHub；单次发布不代表后续修改自动发布。

## 2. 专家、技能与员工的背景色

### 2.1 背景分层

| 层级 / 场景 | 背景与边界 | 使用说明 |
| --- | --- | --- |
| 页面最底层 | `#EDF1FC` | AI Hub 全局浅蓝底色 |
| 顶部导航、左侧栏 | `#FFFFFF` | 左侧栏无外圆角、紧贴左侧，从顶部导航下方延伸到底部 |
| 专家介绍区域 | 浅蓝紫渐变独立板块，22px圆角 | 桌面上下16px、左右32px内边距，最小高度272px；1px白色90%描边，无投影；与下方案例间距16px |
| 专家·技能目录的右侧主区域 | `#FFFFFF`，16px 圆角 | 主视觉、Tab、筛选和目录位于同一白色底面 |
| 专家 / 专家团 / 技能目录卡片 | `#FFFFFF`；`1px solid #E6EBF2`；16px 圆角 | 三种对象共用此卡片外观；悬停不增加或加深外描边，不新增投影 |
| 左侧专家 / 数字员工默认行 | 透明 | 透出白色侧栏，彩色仅集中在图标底座 |
| 左侧专家行悬停 | `#F7F7FF` | 不改变身份图标原有颜色 |
| 左侧专家行选中 | `#F1F1FF` | 标题 `#5552FF`；最近提问时间副标题 `#928FE8` |
| 身份图标底座 | 下述四组渐变，100% 不透明 | 用于角色 / 技能辨识，不代表成功、失败或权限 |

当前数字员工条目是静态展示；切换“数字员工”只改变侧栏名单，不要仅为复用专家样式而添加页面跳转或选中逻辑。

### 2.2 四组身份配色

以 `app.js` 中的 `avatarColors` 为现有实现入口。渐变统一为 **145deg，起点 0%，终点 100%**，内部身份图形统一 `#FFFFFF`。

| 配色键 | 起点 | 终点 | 当前主要业务归属 |
| --- | --- | --- | --- |
| `blue` | `#4C71F5` | `#6AAFFF` | 经营分析、经营诊断、综合运营、车型复盘；现场服务沿用此色 |
| `violet` | `#6550EC` | `#9275FA` | 智慧价格、收益提升；舆情回复沿用此色 |
| `orange` | `#FF8543` | `#FFBC63` | 用户运营、召回、促销；监控值守沿用此色 |
| `teal` | `#0BBCAF` | `#4AD8C8` | 设备诊断、设备运维；竞争洞察沿用此色 |

```js
// 已确认的配色；不要按数组下标或排序位置动态分配。
const avatarColors = {
  blue:   ['linear-gradient(145deg, #4C71F5 0%, #6AAFFF 100%)', '#FFFFFF'],
  violet: ['linear-gradient(145deg, #6550EC 0%, #9275FA 100%)', '#FFFFFF'],
  orange: ['linear-gradient(145deg, #FF8543 0%, #FFBC63 100%)', '#FFFFFF'],
  teal:   ['linear-gradient(145deg, #0BBCAF 0%, #4AD8C8 100%)', '#FFFFFF']
};
```

选中、悬停、无权限时都不要把身份图标统一染成主题紫或灰色。当前目录的无权限状态通过锁图标、文案和灰色“无权限”按钮区分，身份图标保留原色。

## 3. 名称、配色与图标的固定映射

以下文件均位于 `assets/icons/`。Solar 列省略统一前缀 `solar:`；准确来源同时保存在 SVG 根节点的 `data-icon` 中。

### 3.1 专属专家

| 名称 | 配色键 | 本地 SVG | Solar 图标 | 图形含义 |
| --- | --- | --- | --- | --- |
| 经营分析专家 | `blue` | `analysis-filled.svg` | `chart-square-bold` | 柱状图 |
| 智慧价格专家 | `violet` | `price-filled.svg` | `tag-price-bold` | 价格标签 |
| 用户运营专家 | `orange` | `users-filled.svg` | `users-group-rounded-bold` | 用户群 |
| 故障诊断专家 | `teal` | `cardiogram-filled.svg` | `heart-pulse-bold` | 心形心电图 |
| 车型复盘专家 | `blue` | `vehicle-filled.svg` | 用户提供 `TabIcon.svg`，非Solar映射 | 汽车 |

故障诊断专家必须使用用户确认的 `SolarHeartPulseBold.svg` 对应图形。当前本地名为 `cardiogram-filled.svg`；不换回听诊器、普通折线或设备芯片。

车型复盘专家的身份图标使用用户提供的 `TabIcon.svg` 汽车图形，蓝色渐变圆底＋白色汽车。它是身份图标库的明确例外；保留原始路径与 28×28 viewBox，仅把固定黑色转为 `currentColor`。本地文件名和根id保持 `vehicle-filled`，侧栏、目录、任务对象、引用与顶部动效同步更新；不换回方向盘或其他近似汽车图形。介绍、案例与利益点使用日常用户语言；“更正”指提交申请并等待审核，不能写成直接完成修改。三个利益点分别为“查爱车信息 / 看认证状态 / 申请改车型”，对应本地 `benefit-vehicle.svg` / `benefit-vehicle-auth.svg` / `benefit-vehicle-correction.svg`（Solar `wheel-bold` / `shield-check-bold` / `pen-new-square-bold`）。

### 3.2 专家团

| 名称 | 配色键 | 本地 SVG | Solar 图标 | 图形含义 |
| --- | --- | --- | --- | --- |
| 智慧运营专家团 | `blue` | `ops-collaboration-filled.svg` | `share-circle-bold` | 多节点协同 |
| 经营提升专家团 | `violet` | `business-growth-filled.svg` | `graph-up-bold` | 经营增长 |
| 用户增长专家团 | `orange` | `user-growth-filled.svg` | `user-plus-bold` | 新增用户 |
| 设备运维专家团 | `teal` | `maintenance-filled.svg` | `settings-bold` | 运维齿轮 |

“适用场景”筛选中的“设备运维”使用 `maintenance-filled.svg`（Solar `settings-bold`）面型齿轮，16×16px；默认跟随分类灰色，选中跟随主题紫。不要使用细节密集的芯片图形。

### 3.3 技能

| 名称 | 配色键 | 本地 SVG | Solar 图标 | 图形含义 |
| --- | --- | --- | --- | --- |
| 充电量异常根因分析 | `blue` | `root-cause-filled.svg` | `magnifier-bug-bold` | 异常排查 |
| 竞品价格空间分析 | `violet` | `competition-filled.svg` | `radar-2-bold` | 竞品雷达 |
| 用户流失召回 | `orange` | `user-recall-filled.svg` | `user-check-rounded-bold` | 用户回归 |
| 设备故障诊断 | `teal` | `fault-diagnosis-filled.svg` | `bug-bold` | 故障识别 |

目录中这 13 个专家 / 专家团 / 技能各有自己的图形。特别注意：设备运维专家团与设备故障诊断不同；经营提升专家团与竞品价格空间分析不同；故障诊断专家与设备故障诊断技能也不同。

### 3.4 数字员工

| 名称 | 配色键 | 本地 SVG | Solar 图标 |
| --- | --- | --- | --- |
| 经营诊断 | `blue` | `analysis-filled.svg` | `chart-square-bold` |
| 智慧价格 | `violet` | `price-filled.svg` | `tag-price-bold` |
| 促销运营 | `orange` | `promotion-filled.svg` | `ticket-sale-bold` |
| 竞争洞察 | `teal` | `competition-filled.svg` | `radar-2-bold` |
| 现场服务 | `blue` | `station-filled.svg` | `map-point-wave-bold` |
| 设备管理 | `teal` | `device-filled.svg` | `cpu-bolt-bold` |
| 舆情回复 | `violet` | `message-filled.svg` | `chat-round-dots-bold` |
| 监控值守 | `orange` | `monitor-filled.svg` | `monitor-camera-bold` |

这是当前名单的明确映射。跨“专家 / 员工”入口已存在相近业务图形复用，例如经营分析与经营诊断；不据此给目录里的不同能力随意使用重复图形。竞争洞察与竞品价格空间分析当前共用雷达图形，但底色分别为 `teal` 和 `violet`，按上述对象映射保留。

## 4. 图标的形状、尺寸与使用场景

### 4.1 身份图标尺寸

| 位置 | 彩色底座 | 内部图形 | 形状与装饰 |
| --- | --- | --- | --- |
| 左侧专家 / 数字员工列表 | 36×36px | 19×19px | 纯圆，无描边、无高光、无阴影 |
| 专家·技能目录卡片 | 44×44px | 23×23px | 纯圆，无外描边，不增加投影 |
| 任务对话中的专家头像 | 36×36px | 22×22px | 纯圆，使用对应身份渐变 |
| 能力目录顶部循环装饰，桌面 | 56×56px | 28×28px | 纯圆，图形始终保持 0° 正向 |
| 能力目录顶部循环装饰，内容区 ≤720px | 48×48px | 25×25px | 同上 |
| 输入框内当前专家的小图标 | 无彩色底座 | 16×16px | 位于浅紫执行对象标签内，以主题紫显示 |

左侧列表行本身是 **10px 圆角、四边 8px 内边距**；相邻行间距 **6px**；图标与右侧文字间距 **8px**。专家标题与最近提问时间上下间距为 **0**，副标题位于标题下面，不位于图标下面。

顶部循环区是装饰性例外：现有 `.capability-orbit-icon` 保留 `inset 0 1px 0 #FFFFFF 40%` 与 `0 8px 18px #5552FF 约 5%` 的轻微光影。这不适用于侧栏、目录卡片头像和对话头像，不应复制到这些位置。图形随轨道运动但不倾斜；数据来自同一 `capabilityCatalog`，不额外编一套图标映射。

专属专家的时间副标题仅显示最近一次提问的时间，不加“最后使用”前缀。规则：不足1分钟为“刚刚”，不足1小时为“N分钟前”，不足24小时为“N小时前”，不足30天为“N天前”，30天及以上为“M月D日”；只在发送问题或追问时更新。专属专家按最近提问时间倒序排列，最新的在最上面，发送或追问后即时重排；相同时间保持原始顺序，没有时间的排在末尾。数字员工保持原有顺序，只有名称，不渲染时间或空占位；图标与名称垂直居中。实际时间戳本地保存，刷新后恢复并排序；浏览或重新生成不能改变它。

### 4.2 面型、线型与例外

| 用途 | 风格 | 规则 |
| --- | --- | --- |
| 专家 / 专家团 / 技能 / 员工身份图标 | Solar Bold 面型 | 白色实心图形＋对应身份渐变底座 |
| 顶部普通工具、返回、展开收起、搜索等 | Solar Linear 线型 | 默认线宽 1.8，基于 24×24 画布；中性色一般 `#344054` |
| 我的待办、专家·技能主导航 | 默认线型，选中面型 | 默认文字及图标 `#111827`，选中用主题紫 |
| 账户菜单 | Solar Bold 面型 | 右侧三个点也是实心，不使用描边圆点 |
| 顶部介绍中的“利益点”小图标 | Solar Bold 面型 | 五位专家及四个专家团全部统一；按 `benefitIcons` 语义映射，18px、无圆形底座 |
| 开始任务 / 发送箭头 | `composer-send.svg` | 上箭头线宽 **2.0**，是默认 1.8 加粗 0.2 的例外 |
| 停止生成 | `composer-stop.svg` / `solar:stop-bold` | 白色实心方块，位于深色圆形按钮内 |
| 视频封面播放入口 | 本地 `video-play.svg` | 位于封面正中心的白色圆底内，紫色图形22px；不另加右下角箭头入口 |
| 抽屉 / 侧弹窗关闭 | `panel-close.svg` | 深色纯 X，图形24×24px、热区32×32px；无圆形外框、无底色，不以悬停才加深 |
| 模型品牌 | 真实品牌 SVG | GLM、DeepSeek、MiniMax、Qwen 保留自己的品牌配色 |
| 通用模型 / 模型管理 | 盒子形 | `composer-model.svg` 当前为线框盒子；`account-models.svg` 为面型盒子，两者不混用 |

顶部利益点当前为短标签：高度至少36px、圆角8px、无描边，纯白 `#FFFFFF` 填充，投影为 `0 3px 10px #5552FF 7%`；图标18px、Solar Bold面型，无额外圆底；文字14px、600字重、`#667085`。图标按 `benefitIconColors` 固定映射：橙 `#F39A38`、蓝 `#3F96EB`、青绿 `#20B295`、金橙 `#E9A13A`、青蓝 `#1AA9BA`、玫瑰粉 `#F07891`；同语义保持相同颜色，不按排列位置变色，不套用身份图标渐变或主题紫。不恢复为44px身份头像，不重新加入已删除的详细说明。

故障诊断专家的前两个利益点明确使用简洁实心图形：`benefit-diagnosis.svg` / Solar `danger-triangle-bold`（橙色），`benefit-offline.svg` / Solar `cloud-cross-bold`（蓝色）；不恢复芯片引脚或断网弧线造型。身份头像仍用原心形心电图，第三项工单图标不变。

“专家能帮你做点什么？”标题图标是用户指定的 **MingCute `message-3-ai-fill`**，本地文件 `expert-ai-message.svg`，24px；内部渐变为 `#5552FF → #8C52FF`，图文间距6px。这是明确保留的图标库例外，不需要为了“统一 Solar”而替换。

### 4.3 本地 SVG 交付规则

检索入口：[Yesicon / Solar](https://yesicon.app/zh-Hans/solar)。同源数据：[Iconify Solar 集合](https://github.com/iconify/icon-sets/blob/master/json/solar.json)。Solar 作者为 **480 Design**，授权为 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)。模型品牌图标采用 [LobeHub Icons](https://github.com/lobehub/lobe-icons)，授权与现有来源记录见项目 README。

新增 / 替换时按以下顺序执行：

1. 先按功能含义选图，确定面型或线型，再检查是否与目录中的其他不同功能重复。
2. 下载 SVG 到 `assets/icons/`；每个图标一个文件，英文小写短横线命名。
3. 保留真实路径、`viewBox`、`defs`、渐变 / 遮罩引用；根节点加入同文件名的 `id` 与真实来源 `data-icon`。
4. 普通单色图标使用 `currentColor`。Bold 图标依靠原始填充路径，不通过给线型图标强行加 `fill` 伪造面型。
5. 保留品牌图标和用户指定渐变图标的原有颜色，不被全局 `fill: currentColor` 覆盖。
6. 用本地 `<use>` 或现有 `iconUrl()` / `iconMarkup()` 引用，禁止直接引用远端 SVG；用 HTTP 预览确认无404。
7. 更新本文映射表及项目来源记录。替换同一个功能时尽量保留文件名与根节点 id。

```html
<!-- 已存在于本地 assets/icons/analysis-filled.svg 的文件结构 -->
<svg xmlns="http://www.w3.org/2000/svg"
     id="analysis-filled" viewBox="0 0 24 24"
     data-icon="solar:chart-square-bold">
  <!-- 此处保留下载文件的真实路径，不自行近似重画。 -->
</svg>

<!-- 页面中引用，图形颜色由外层 color 决定 -->
<svg class="icon" aria-hidden="true">
  <use href="./assets/icons/analysis-filled.svg#analysis-filled"></use>
</svg>
```

```js
iconUrl('analysis-filled'); // ./assets/icons/analysis-filled.svg#analysis-filled
iconMarkup('analysis-filled'); // 生成指向该本地 SVG 的引用
```

Logo、favicon 是单独的用户品牌资产；不要当作可替换的功能图标。气泡尖角属于组件装饰，现有 CSS 内嵌矢量不是角色图标来源，不能据此改回远端图标或内嵌整套图标集。

## 5. 全局颜色规范

### 5.1 基础设计变量

以下变量已存在于 `style.css :root`，新组件优先复用，不另起近似色。

| 现有变量 | 值 | 语义 / 范围 |
| --- | --- | --- |
| `--page-bg` | `#EDF1FC` | 页面底色、底部悬浮输入框外的渐隐终点 |
| `--surface` | `#FFFFFF` | 导航、侧栏、内容卡片、弹层、输入框底色 |
| `--primary` | `#5552FF` | 主题色、导航选中、主操作、链接、AI相关小图标 |
| `--primary-muted` | `#928FE8` | 选中行的浅紫副标题、选中筛选的数量 |
| `--primary-soft` | `#F1F1FF` | 选中行、轻量主按钮底色 |
| `--primary-hover` | `#F7F7FF` | 普通导航 / 列表的轻微悬停底色 |
| `--primary-border` | `#D5D4FF` | 需要主色反馈的输入 / 选择控件边界；不用于案例选中态 |
| `--text` | `#111827` | 主标题、重点文字、主导航默认图标、关闭按钮 |
| `--body-text` | `#344054` | 常规正文、默认列表标题 |
| `--icon-color` | `#344054` | 白底普通工具图标；不要调成过浅的灰 |
| `--muted` | `#667085` | 辅助说明、非选中Tab、目录描述 |
| `--subtle` | `#98A2B3` | 弱提示、元信息、默认专家最近提问时间 |
| `--border` | `#E6EBF2` | 常规控件与能力卡片描边 |

`#F0F1F6` 是账户菜单分组分割线的指定颜色，使用 **`1px solid #F0F1F6`**；不要把所有有意保留的控件边框都替换成此色。

### 5.2 渐变与光影

| 使用位置 | 颜色 / 角度 | 限制 |
| --- | --- | --- |
| 开始任务、发送按钮、视频空态播放圆底 | `linear-gradient(135deg, #5552FF, #8C52FF)` | 白色图形 / 文案；不是身份图标的145deg渐变 |
| 案例区标题的 AI 消息图标 | 同色端点，左上→右下 | 渐变在 SVG 内定义 |
| 能力目录主视觉的“技能”文字 | `linear-gradient(100deg, #5552FF, #8C52FF)` | 当前标题局部例外；不要将全部正文改成渐变字 |
| 专家身份底座 | 第2.2节四组145deg渐变 | 保持明亮、饱满，不使用灰脏的低饱和底色 |
| 顶部导航投影 | `0 4px 18px rgb(31 45 83 / 5%)` | 浅、柔，不新增边界硬线 |
| 专家介绍背景 | `linear-gradient(112deg, #FCFCFF 0%, #F3F5FF 35%, #E8F0FF 68%, #E4EAFF 100%)` | 独立浅色圆角板块，局部柔白与淡青补光；不使用深紫色大块填充 |

顶部背景纹理使用本地 `assets/expert-charging-texture.png`，经网关参考用户确认图片生成，包含简化楼宇方块、车辆与充电桩，位于机器人后方及板块下部。造型为低细节、半透明、哑光的浅蓝紫色块，不使用写实汽车、街景、复杂建筑或反射材质。整体不透明度65%，左侧与顶部渐隐，背景层下移20px。机器人与气泡作为一组左移20px、组内保持12px间距；内容区≤900px时左移12px，≤600px时取消偏移。纹理只作装饰，不承载信息、不接受点击，不属于Solar图标库；不覆盖正文与机器人。内容区不超过1100px时板块上下20px、左右24px内边距、最小高度244px；不超过600px时四边20px、高度随内容自然撑开、纹理30%不透明度。

### 5.3 关键组件颜色与状态

| 组件 / 状态 | 背景 | 文字 / 图标 / 边界 |
| --- | --- | --- |
| 左侧专家选中行 | `#F1F1FF` | 标题 `#5552FF` / 600；副标题 `#928FE8`；身份头像保持原色 |
| 我的待办选中角标 | `#5552FF` | 数字 `#FFFFFF` |
| 身份Tab轨道 | `#F3F5F7` | 默认文字 `#667085` |
| 身份Tab选中项 | `#FFFFFF` | 文字 `#5552FF`；已有轻微Tab投影不用于身份头像 |
| 顶部范围类型标签（公司 / 大区） | `#F1F1FF` | 文字 `#5552FF`，12px / 500，4px圆角；类型随实际选择更新 |
| 能力目录标签 | `#F5F6FA` | 文字 `#667085` |
| 能力“使用”按钮 | `#F1F1FF`；悬停 `#E6E5FF` | 文字 / 箭头 `#5552FF` |
| 能力“无权限”按钮 | `#F6F7FA`；悬停 `#ECEEF4` | 文字 / 锁图标 `#667085`，保留明确文案 |
| 普通案例入口 | `#FAFBFF`、1px `#EFF0F8`描边；悬停 `#F3F2FF` | 正文 `#344054`；右侧箭头 `#5552FF`、圆底 `#EEEDFF`；点击后无持续选中态 |
| 独立视频封面 | 浅蓝紫渐变 `#DFE2FF → #E9EFFF → #DCE9FF`、1px `#FFFFFF`描边 | 正中心白色圆形播放按钮、紫色播放图形；对应专家面型图标弱化为背景装饰，名称与当前专家联动 |
| 顶部机器人气泡 | `#FFFFFF 70%` | 1px纯白描边、16px圆角；“Hi! 👋”为18px / 600主题紫，正文15px / 600、`#344054`；固定问候，不可点击、无轮播、无星星或操作箭头 |
| 输入框外层 | `#FFFFFF` | 无外描边、无阴影；正文 `#344054` |
| 输入框占位文字 | 透明底 | `#9AA2B4` |
| 添加 / 麦克风按钮 | `#F6F7FA` | 默认图标 `#344054`；悬停 / 激活时浅紫底＋主题紫 |
| 输入框执行对象标签 | `#F2F1FF` | 文字 / 图标 `#5552FF` |
| 输入框普通范围标签 | `#F6F7FA` | 文字 `#747E90` |
| 发送按钮可用 | 135deg主题渐变 | 文字 / 图标 `#FFFFFF` |
| 发送按钮禁用 | `#EDEEF5` | 文字 / 图标 `#B8BBCC` |
| 停止生成按钮 | `#111827` | 白色实心方块；38×38px纯圆 |
| 账户兜底头像 | `#5552FF` | 姓名首字 `#FFFFFF`；36px纯圆 |
| 退出登录 | 透明；悬停 `#FFF1F2` | 文字 / 图标 `#DC2626`；上方分割线 `#F0F1F6` |

### 5.4 业务状态色

这些颜色表达状态，不能代替专家身份配色。状态需要同时显示文案，不只依靠颜色区分。

| 当前组件用途 | 颜色 | 注意 |
| --- | --- | --- |
| 待办 / 运行记录：成功 | `#24A184` | 成功状态文字和圆点 |
| 待办 / 运行记录：失败 | `#D86570` | 失败状态，不是删除操作色 |
| 待办 / 运行记录：待处理 | `#C19543` | 待审批 / 待处理状态 |
| 待办 / 运行记录：运行中 | `#7770DC` | 表示运行中 |
| 表格行：立即执行 | `#5552FF` | 主操作文字 |
| 表格行：暂停 / 普通操作 | `#475467` | 深灰操作文字 |
| 表格行：删除 | `#E5484D` | 删除文字操作，不加悬停下划线 |
| 账户菜单：退出登录 | `#DC2626` | 当前明确指定的红色警示操作 |
| 任务结果表格：负向变化 | `#E35B67` | 仅用于该结果表的负向数值 |

当前代码保留了不同组件的语义色细分；本文没有将其伪装成统一的 `--success` / `--danger` Token。后续若要统一这些颜色，应作为独立设计变更处理，不顺手全局替换。

## 6. 与配色相关、必须一起保留的组件规则

| 项目 | 已确认规则 |
| --- | --- |
| 主内容区四周留白 | 20px；不是旧版24px或12px |
| 顶部工具入口 | 最近任务、通知、反馈保留胶囊，悬停 / 打开为浅紫底与主题紫；使用右侧480px抽屉、20px内容内边距，窄屏内边距16px；沿用侧栏移入移出与纯X规则 |
| 我的待办 / 定时任务标题 | 仅保留主Tab，不展示下方介绍性副标题；与分类 / 筛选栏间距20px |
| 侧栏内部 | 四边8px；侧栏本体无圆角，不再悬浮成卡片 |
| 顶部品牌区 | Logo在顶部导航内，高44px；可见图形左缘15px；无Logo右侧竖线和下方横分割线 |
| 顶部范围选择 | 桌面320×38px；框内按“公司 / 大区标签＋名称＋下拉箭头”排列，名称14px / 600。标签20px高、水平内边距6px；窄屏保留类型并省略长名称，浏览列表Tab不改变当前选择 |
| 专家介绍 | 桌面上下16px、左右32px；大标题44px / 600，直接用专家名称；不重复放左侧身份头像 |
| 专家介绍主副标题 | 间距4px；介绍文字15px / 26px，颜色 `#667085` |
| 案例外层白卡 | 22px圆角、四边16px；标题20px / 600，标题与入口间距16px |
| 案例和视频排列 | 桌面两张独立卡片、间距16px、顶部对齐；左侧四个案例两列两行，右侧视频约36%宽、限制340–480px且保持16:9；内容区≤900px卡片上下排列，视频满宽、高240px、不限制比例，≤500px案例单列 |
| 案例入口 | **固定12px圆角**；最小高76px、四边16px；文案14px / 22px、最多两行，点击回填完整内容；右侧28px浅紫圆底箭头 |
| 视频封面 | 22px圆角、1px纯白描边；并排时16:9、最大宽480px，独占一行时满宽、高240px、比例自适应；播放按钮44–56px、固定正中心、位于装饰层之上，主副标题居底，副标题14px / 22px；左上角距上/左24px，左下角距下/左/右24px；整卡点击打开视频弹窗 |
| 换一批 | 标题行右侧、主题紫；每次展示四个案例，不覆盖输入草稿，独立视频卡片保持不动 |
| 输入框 | 最大宽1000px、居中贴主内容区底部；白底、24px圆角、四边16px、无外边框和阴影 |
| 输入框上方渐隐 | 48px，从页面底色透明端过渡到 `#EDF1FC`，内容可滚到输入框下面，不形成硬截断 |
| 发送 / 停止状态 | 正常胶囊高38px；生成中收成38px圆形。宽度过渡260ms，背景180ms，图标交叉淡入淡出；点击停止立即生效 |
| 键盘焦点外描边 | 当前Demo按评审要求已移除，不能重新出现默认蓝色外圈；保留原生按钮、焦点与键盘操作能力 |

发送按钮的背景叠层只通过外层圆角裁切一次，内部背景层不再重复圆角。禁用灰色态和停止深色态切换完成后，紫色渐变层透明度必须为0；不要用带圆角的灰色遮罩覆盖仍然可见的紫色底，否则抗锯齿边缘会漏出紫色细线。

## 7. 给开发 AI 的实现定位与验收

### 7.1 修改入口

| 内容 | 当前入口 |
| --- | --- |
| 全局基础颜色 | `style.css` 的 `:root` |
| 四组身份渐变 | `app.js` 的 `avatarColors` |
| 专家 / 员工 / 专家团映射 | `app.js` 的 `groups.expert`、`groups.employee`、`groups.team` |
| 专家·技能目录映射 | `app.js` 的 `capabilityCatalog` |
| 介绍区利益点小图标 | `app.js` 的 `benefitIcons` |
| 侧栏列表身份样式 | `.person-row`、`.person-avatar`、`.person-copy` |
| 目录卡片身份样式 | `.capability-card`、`.capability-avatar` |
| 顶部循环图标 | `.capability-orbit-*`，复用目录数据 |
| 案例 / 视频样式 | `.expert-task`、`.expert-video-card`、`.expert-resources` |
| 案例回填 | `fillExpertTask()`；只回填，不创建持续选中状态 |
| 发送 / 停止动画 | `updateComposerControls()`、`.composer-send`及其内容 / 停止图标层 |
| SVG资源与来源 | `assets/icons/*.svg` 的文件名、根id、`data-icon` |

新增对象时同时检查相应名单、目录和其他入口。现有 `groups` 中 `icon` 多为基础名，渲染时追加 `-filled`；`capabilityCatalog.icon` 已是完整本地名，不能再次追加成 `-filled-filled`。

### 7.2 验收清单

- [ ] 页面底色、白色卡片、彩色图标底座三层区分正确。
- [ ] 所有已列出的身份颜色、图标与第3节映射一致，搜索和排序不改变颜色。
- [ ] 专家、专家团、技能身份图标是面型；所有利益点说明图标也使用面型，但不增加身份圆底。
- [ ] 左侧图标36px纯圆、内图19px，无描边、高光和投影；目录头像保持44px。
- [ ] 能力目录的13个对象没有因图省事使用重复的通用图形。
- [ ] 普通白底工具图标足够深，选中行的副标题为浅紫，身份头像不被选中态覆盖。
- [ ] 案例点击能正确回填，点击后没有紫色描边、选中底色或 `aria-pressed`。
- [ ] 左侧四个案例两列两行，右侧独立视频封面；封面名称和图标跟随专家，点击不会回填输入框。
- [ ] 案例入口始终12px圆角矩形；案例外层白卡与独立视频卡均为22px，未被全局圆角规则误改。
- [ ] SVG全部本地加载，资源存在、根id正确、无404；品牌 / 渐变图标未被统一染色。
- [ ] 发送、等待、停止、完成四种状态的颜色及按钮形态正确，动画不中断停止操作。
- [ ] 系统减少动态效果时关闭相关动效；未重新添加焦点外圈。
- [ ] 未擅自修改业务行为、其他Demo、上传范围或GitHub部署。

本文只固定已经确认的视觉基线。发现当前代码与本文冲突时，先定位具体组件和最近变更，不通过全局替换“统一”所有灰色、紫色、圆角或图标尺寸。
